const INITIAL_STATE = [
  { name: '',
    score: 0,
    picture: '' },
];

const ranking = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default ranking;
